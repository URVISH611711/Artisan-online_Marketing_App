# ─── Web framework ───────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart>=0.0.9

# ─── Database ────────────────────────────────────────────────────
SQLAlchemy==2.0.28
alembic==1.13.1
psycopg2-binary==2.9.9

# ─── Config & validation ─────────────────────────────────────────
pydantic==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1
email-validator>=2.1.1

# ─── Auth ────────────────────────────────────────────────────────
PyJWT>=2.8.0
passlib==1.7.4
argon2-cffi>=23.1.0          # backend for CryptContext(schemes=["argon2"])

# ─── Storage ─────────────────────────────────────────────────────
supabase>=2.0.0

# ─── Local AI stack ──────────────────────────────────────────────
# !! FROZEN VERSION SET — DO NOT UPGRADE IN PLACE !!
#
# diffusers 0.27.2 top-level-imports `cached_download` from huggingface_hub
# (diffusers/utils/dynamic_modules_utils.py). huggingface-hub >= 0.26 REMOVED
# that symbol, and transformers >= 4.49 requires hub >= 0.26. So bumping
# transformers here breaks `from diffusers import StableDiffusionInpaintPipeline`
# with an ImportError, taking down the WORKING SD inpainting pipeline.
#
# transformers is used by diffusers internally, no vision_service sidecar needed.
transformers==4.39.3
huggingface-hub==0.22.2
diffusers==0.27.2
accelerate>=0.28.0
torch
torchvision
einops
scikit-image
faster-whisper>=1.0.1

# ─── Image processing ────────────────────────────────────────────
pillow
opencv-python-headless>=4.9.0   # contact-shadow blur/warp in image_compositor
